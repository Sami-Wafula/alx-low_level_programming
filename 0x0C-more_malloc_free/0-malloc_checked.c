#include "main.h"
#include <stdlib.h>
/**
 * *malloc_checked - memory allocation
 * @b: int
 * Return: 0.
 */
void *malloc_checked(unsigned int b)
{
	str = malloc(sizeof(int) * b);
	printf("%\n", str);
	return (0);
}
