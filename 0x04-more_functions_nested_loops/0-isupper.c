#include "main.h"
#include <stdio.h>

/**
 * _isupper: check if letter is upper case
 * @c: c is an ascii character
 * Return: Always 0 if true, 1 if false
 */
int main(void)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (0);
	}
	else
	{
		return (1);
	}
}




